# yolo_rect_annotator
A simple **Python + OpenCV** tool for annotating **rectangular bounding boxes (BBOX)** in video frames and exporting them in YOLO format.   After drawing each box, you type the numeric class ID in the terminal.
